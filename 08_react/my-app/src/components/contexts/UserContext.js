import React, { createContext } from 'react'; // context 을 만드는 거을 import 하겠다.

// createContext(초기값) : Context 생성
const UserContext = createContext(); // 외부에다가 만든 전역 변수

export default UserContext;


